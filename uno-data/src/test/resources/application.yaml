spring:
  jpa:
    hibernate:
      ddl-auto: create # 自动执行创表语句
    database: mysql
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
  profiles:
    include: db

mybatis:
  tab